PORT=4000
MONGODB_URI=
# Toggle Firebase token verification (prototype). If empty/false, auth is optional.
ENABLE_FIREBASE_AUTH=
# Firebase Admin credentials (optional, if enabling verification)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=""
